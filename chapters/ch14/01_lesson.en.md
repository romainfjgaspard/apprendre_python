# 📑 Chapter 14 — Read CSV files

<img src="images/heat/circuit-gb.jpg" width="280" alt="Silverstone" style="margin: 6px; vertical-align: middle;"/>

**What you will learn:**
- What a **CSV** file is (one line = one row, columns separated by `,`)
- Read a CSV with `open()` + `split()` (no module needed)
- Use the built-in `csv` module (cleaner)
