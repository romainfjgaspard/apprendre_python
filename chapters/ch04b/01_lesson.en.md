# 🔄 Chapter 4b — The while Loop

The `while` loop repeats code **as long as a condition is True** — like Scratch's **"repeat until"** block (but inverted: while = as long as).

**What you will learn:**
- `while` loop — repeat until a condition becomes False
- `break` — escape a loop early when a condition is met
