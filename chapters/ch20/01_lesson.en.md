# 🔢 Chapter 20 — numpy: Fast Math with Arrays

**What you will learn:**
- `numpy.array` — a super-powered list for numbers
- Vector operations: add, multiply, modulo
- Why numpy is **much faster** than Python loops

> ⚠️ `pip install numpy` if not already installed