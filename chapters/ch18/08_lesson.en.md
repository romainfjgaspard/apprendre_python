---
## ✏️ Exercise — Build a Pokédex with classes

Create a `Pokemon` class with:
- `__init__` that stores name, hp, attack, ptype
- a `battle_cry()` method that prints `"<name> uses <attack_name>!"`
- `__str__` to display the Pokémon nicely

Then create a list of 3+ Pokémon and loop through them.