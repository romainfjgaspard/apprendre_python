---
## 📖 Lesson 2 — Add and remove items

| Method | What it does |
|--------|------------|
| `list.append(x)` | Add `x` at the end |
| `list.remove(x)` | Remove the first `x` found |
| `list.pop()` | Remove and return the last item |
| `list.insert(i, x)` | Insert `x` at position `i` |