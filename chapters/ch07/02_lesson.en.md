---
## 📖 Lesson 1 — `def` and `return`

```python
def function_name(parameters):
    # code inside the function
    return result
```

- `def` = "I am defining a function"
- Parameters = inputs the function receives
- `return` = the result sent back
- Indentation (4 spaces) = everything inside the function