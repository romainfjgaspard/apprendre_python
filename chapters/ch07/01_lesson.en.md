# 🧩 Chapter 7 — Functions

A **function** is a reusable block of code — like a **custom block** in Scratch!

**What you will learn:**
- `def` — define a function
- Parameters and `return`
- Default values for parameters
- Functions that call other functions
- Build the Pokémon battle system!