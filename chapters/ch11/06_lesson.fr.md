---
## 📖 Leçon 3 — Filtrer avec une compréhension

Tu veux seulement les pilotes Ferrari ? Utilise une **compréhension de liste** avec `if` :
```python
ferrari = [d for d in standings if d['team'] == 'Ferrari']
```
