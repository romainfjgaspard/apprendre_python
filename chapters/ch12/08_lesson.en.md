---
## ✏️ Exercise — Race simulator

Simulate a 50-lap race for 5 drivers. Each lap, every driver gets a random
lap time between 80.0 and 85.0 seconds. Find the **total time** per driver and
rank them. Use `random.seed(7)` for reproducible results.