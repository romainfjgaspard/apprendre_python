---
## 📖 Leçon 1 — `if` et `else`

```python
if condition:
    # s'exécute si True
else:
    # s'exécute si False
```

⚠️ N'oublie pas les **deux-points** `:` à la fin et l'**indentation de 4 espaces** !
